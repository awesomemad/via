[package]
name = "via-cdylib"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "via_plugin"

[dependencies]
via-engine = { path = "../via-engine" }
serde_json = "1.0"
anyhow = "1.0"
