[package]
name = "via-mcp"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "via-mcp"
path = "src/main.rs"

[dependencies]
via-engine = { path = "../via-engine" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
